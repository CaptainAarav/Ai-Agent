[project]
name = "ai-agent"
version = "0.1.0"
description = "A simple CLI coding ai agent that can be used with any LLM api."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai==2.44.0",
    "python-dotenv==1.1.0",
    "requests>=2.34.2",
    "rich>=15.0.0",
]
